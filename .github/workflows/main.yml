name: 15-Day Continuous VPS

on:
  workflow_dispatch: 
  schedule:
    - cron: '0 */5 * * *' 

env:
  # Opts into the updated runner environment to clean up platform warnings
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  run-vps:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure System & SSH Server
        run: |
          sudo apt-get update
          sudo apt-get install -y openssh-server build-essential curl wget screen zip unzip
          sudo mkdir -p /var/run/sshd
          
          # Set the SSH login credentials
          echo 'root:VpsPassword2026!' | sudo chpasswd
          sudo sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo service ssh restart
          
          mkdir -p /home/runner/workspace

      - name: Install & Configure Ngrok
        run: |
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com/ buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt-get update && sudo apt-get install ngrok
          
          # Safety check: if secret is empty, fail with a clear message instead of an ugly syntax crash
          if [ -z "${{ secrets.NGROK_AUTH_TOKEN }}" ]; then
            echo "ERROR: NGROK_AUTH_TOKEN secret is missing in your GitHub Settings!"
            exit 1
          fi
          ngrok config add-authtoken "${{ secrets.NGROK_AUTH_TOKEN }}"

      - name: Restore Previous Session Data
        run: |
          cd /home/runner
          if [ ! -z "${{ secrets.BACKUP_DOWNLOAD_URL }}" ]; then
            echo "Found previous backup. Restoring files..."
            # Using -k / --no-check-certificate to prevent any unexpected SSL blocks on download
            wget --no-check-certificate -O backup.zip "${{ secrets.BACKUP_DOWNLOAD_URL }}" || echo "No active backup found online yet."
            if [ -f backup.zip ]; then
              unzip -o backup.zip -d /home/runner/workspace
              rm backup.zip
            fi
          else
            echo "First-time setup or no backup URL configured. Starting fresh."
            echo "# Put your project files inside /home/runner/workspace to keep them safe" > /home/runner/workspace/README.txt
          fi

      - name: Start Network Tunnel & Run Cycle
        run: |
          screen -dmS ngrok_tunnel ngrok tcp 22
          
          echo "===================================================="
          echo " VPS ONLINE AND RUNNING NORMAL"
          echo "===================================================="
          echo "1. Go to: https://dashboard.ngrok.com/endpoints/status"
          echo "2. Copy the TCP address (e.g., 0.tcp.ngrok.io:12345)"
          echo "3. Connect in your terminal using:"
          echo "   ssh root@your-ngrok-host -p your-ngrok-port"
          echo "   Password is: VpsPassword2026!"
          echo "===================================================="
          echo "IMPORTANT: ALWAYS WORK INSIDE: /home/runner/workspace"
          echo "===================================================="
          
          DURATION=18000
          while [ $DURATION -gt 0 ]; do
            echo "VPS status: Active. Time remaining in this shift: ${DURATION}s"
            sleep 300
            DURATION=$((DURATION - 300))
          done

      - name: Automated Session Backup
        if: always() 
        run: |
          echo "5-hour shift ended. Packaging workspace..."
          cd /home/runner
          if [ -d "workspace" ]; then
            zip -r backup.zip workspace
            
            # Using Pixeldrain API instead since Bashupload has an expired SSL configuration
            UPLOAD_LOG=$(curl -k -F "file=@backup.zip" https://pixeldrain.com/api/file/)
            FILE_ID=$(echo "$UPLOAD_LOG" | grep -o '"id":"[^"]*' | grep -o '[^"]*$')
            
            if [ ! -z "$FILE_ID" ]; then
              DOWNLOAD_LINK="https://pixeldrain.com/api/file/$FILE_ID"
              echo "===================================================="
              echo "BACKUP SUCCESSFUL"
              echo "Update your repository secret 'BACKUP_DOWNLOAD_URL' with this link:"
              echo "$DOWNLOAD_LINK"
              echo "===================================================="
            else
              echo "Backup file upload failed."
            fi
          fi
